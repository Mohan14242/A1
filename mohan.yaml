- name: installing the playbooks 
  hosts: localhost 
  become: yes 
  vars:
    packages: 
      nginx: present
  tasks: 
  - name: installing the packages 
    yum: 
      name: "{{item.key}}"
      state: "{{item.value}}"
    with_dict: "{{packages}}"


